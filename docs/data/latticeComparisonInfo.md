# Lattice Comparison

Shows the main structure's lattice vectors (lengths and angles) against the comparison/overlay
structure(s), side by side with their differences — one radar plot + table per structure, stacked
in a scrollable list when there's more than one — so you can quickly see how much a relaxation or
transformation changed the cell.
