# Histogram

A live bond-length distribution for the currently shown bond categories. Drag **Bins** to change
the bar count and **Max Dist** to change the plotted range; hover a bar for its exact range and
count.
