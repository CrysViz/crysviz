{
  "name": "crysviz-dev-tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Dev-only lint/typecheck tooling for CrysViz. NOT bundled into the app — the app in docs/ still loads raw ES modules with no build step. These devDependencies and node_modules are never served (the GitHub-Pages deploy only publishes docs/).",
  "scripts": {
    "lint": "eslint docs",
    "lint:fix": "eslint docs --fix",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check-imports": "python3 tools/check_imports.py",
    "check": "npm run lint && npm run check-imports"
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "eslint-plugin-unused-imports": "^4.1.4",
    "globals": "^15.11.0",
    "typescript": "^5.6.3"
  }
}
