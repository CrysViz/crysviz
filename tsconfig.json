{
  "//": "Type-checking config for CrysViz (plain JS via tsc --checkJs). Emits nothing.",
  "//2": "Intentionally lenient (strict:false) for this mostly-untyped codebase, but kept at ZERO errors and enforced as a CI gate via `make check` (.github/workflows/check.yml). Tighten per file with JSDoc types over time; see docs/globals.d.ts and the three.module.d.ts/CSS2DRenderer.d.ts stubs for the project-wide relaxations.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable", "WebWorker"],
    "allowJs": true,
    "checkJs": true,
    "noEmit": true,
    "strict": false,
    "noImplicitAny": false,
    "skipLibCheck": true,
    "allowImportingTsExtensions": false,
    "types": []
  },
  "include": ["docs/**/*.js", "docs/globals.d.ts"],
  "exclude": [
    "docs/external/**",
    "docs/compiled/**",
    "old/**",
    "node_modules"
  ]
}
